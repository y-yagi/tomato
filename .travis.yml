language: go

go:
  - 1.8
  - 1.9

install:
  - go get -u github.com/golang/lint/golint
  - go get -u github.com/golang/dep/cmd/dep
  - dep ensure

before_script:
  - go vet
  - golint -set_exit_status

script:
  - go test -v
